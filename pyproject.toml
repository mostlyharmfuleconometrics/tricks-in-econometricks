[project]
name = "tricks-in-econometricks"
version = "0.1.0"
description = "Executable econometrics notes built with Quarto"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "jupyter>=1.1.1",
  "matplotlib>=3.10.5",
  "numpy>=2.3.2",
]

[dependency-groups]
dev = []
